Anotações
Valores imutáveis quando copiados para outra variável eles guardam o valor, no caso de valores mutáveis é feito a referência do local da memória, ou seja, no primeiro caso é possivel atribuir e mudar os valores em cada variável, já no segundo caso, se mudar qualquer um ele muda todos.

Mutáveis (passados por referência)
  list
  set
  dict
  class (o usuário pode mudar isso)
  ...

Imutáveis (copiados)
  bool
  int
  float
  tuple
  str
  ...